# Production extras. NOT installed in the sandbox venv -- that is deliberate.
# Installing these does not by itself enable production mode; AIS_FMD_ENV must
# also be set to exactly "production".
-r requirements.txt
supabase>=2.15
openai>=1.0
# Powers auth.login_gate's Google sign-in path (st.login/st.user, Streamlit's
# own OIDC support -- see ais_fmd/auth.py). Not needed in sandbox: the whole
# login_gate function returns immediately there.
Authlib>=1.3.2
